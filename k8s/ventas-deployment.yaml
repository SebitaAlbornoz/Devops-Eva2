apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-ventas
  namespace: innovatech-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-ventas
  template:
    metadata:
      labels:
        app: backend-ventas
    spec:
      imagePullSecrets:
        - name: ecr-secret
      containers:
        - name: backend-ventas
          image: 128806834448.dkr.ecr.us-east-1.amazonaws.com/db-despacho:eks-v1
          ports:
            - containerPort: 8080
          env:
            - name: DB_ENDPOINT
              value: "172.17.0.1"
            - name: DB_PORT
              value: "3307"
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: ventas-secret
                  key: DB_NAME
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: ventas-secret
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ventas-secret
                  key: DB_PASSWORD
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
